html body
  margin: 0
  padding: 0
  background-color: white

@import '../../utils/mixin.styl'

// 设置placeholder的颜色
::-webkit-input-placeholder { /* WebKit browsers */
    color:    #B7B7B7;
}
:-moz-placeholder { /* Mozilla Firefox 4 to 18 */
    color:    #B7B7B7;
}
::-moz-placeholder { /* Mozilla Firefox 19+ */
    color:    #B7B7B7;
}
:-ms-input-placeholder { /* Internet Explorer 10+ */
    color:    #B7B7B7;
}


input, button, select, textarea {
  outline:none;
}

textarea {
  resize: none;
}

 // 去掉input disabled默认样式
input:disabled{
  background-color : #fff; //修改默认灰色样式
  color: #666;      
  opacity:1; //默认的不透明级别为0.3
  -webkit-text-fill-color:#666; //字体颜色安卓与IOS适配  
  -webkit-opacity:1;   //不透明级别安卓与IOS适配 
}

// 去掉img默认样式
img[src=''], img:not([src]) {
  opacity: 0;
}

// 列表容器的样式，主要定义bottom
.product-list-container
  margin-bottom: 1.333333rem 

// 提示的样式
.tips-container
  position: fixed
  top: 0
  left: 0
  right: 0
  bottom: 0
  text-align: center
  div
    position: relative
    text-align: center
    width: 6.8rem
    margin: 0 auto
    padding: 0.44rem
    background-color: rgba(51,51,51,0.7)
    border-radius: 0.066667rem
    color: white
    font-size: 14px
    top: 9.92rem
    
 #app
  .area_btn[data-v-f1971c36]
    color: #FBD959
  .area_ctrl[data-v-f1971c36]
    background-color: white